[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "gpac"
version = "0.1.0"
authors = [
  { name="Yusuke Watanabe", email="ywatanabe@alumni.u-tokyo.ac.jp" },
]
description = "GPU-Accelerated Phase-Amplitude Coupling calculation using PyTorch"
readme = "README.md"
license = { file="LICENSE" }
requires-python = ">=3.8"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License", # Or your chosen license
    "Operating System :: OS Independent",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = [
    "torch>=1.9.0",
    "numpy",
    "scipy",
    "torchaudio>=0.9.0", # Specify a version known to have sinc_impulse_response if needed
]

[project.urls]
"Homepage" = "https://github.com/[your_username]/gPAC" # Replace
"Repository" = "https://github.com/[your_username]/gPAC" # Replace
"Bug Tracker" = "https://github.com/[your_username]/gPAC/issues" # Replace
